# Crop Disease Library

This folder contains a manually curated mini-library of crop diseases and reference images.

- `diseases.json` stores disease metadata and relative image paths.
- `images/` contains reference photos for visual indication.

You can extend this library by adding new entries in `diseases.json` and placing the matching image file in `images/`.
